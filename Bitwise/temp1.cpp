#include <iostream>
using namespace std;

/*
The symbol '^' is the bitwise XOR operation.
It is not the symbol for exponentiation. For exponentiation, we can
use the pow() function in math.h
*/

int main()
{
	int c = 2 ^ 3;
	cout << c << endl;

	return 0;
}
